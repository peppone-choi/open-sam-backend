/**
 * City DTO
 * TODO: Yup 스키마 작성
 */

// TODO: Yup import
// import { object, string, number } from 'yup';

/**
 * 도시 생성 DTO
 */
// export const CreateCitySchema = object({
//   body: object({
//     name: string().required(),
//     level: number().min(1).max(10).default(1),
//     pop: number().min(0).required(),
//     agri: number().min(0).required(),
//     // ... 기타 필드
//   }),
// });

// export type CreateCityDto = InferType<typeof CreateCitySchema['body']>;
