# 개발용 Dockerfile - 핫 리로드 지원
FROM node:20-alpine

WORKDIR /app

# 의존성 설치
COPY package*.json ./
RUN npm install

# 소스는 볼륨 마운트로 제공됨
# ts-node-dev가 파일 변경 감지

EXPOSE 8080

# 기본 커맨드 (docker-compose에서 override 가능)
CMD ["npm", "run", "dev:api"]
